# Spring World 内容哲学

**目标：知道原理，了解背景，会使用。**

三者缺一不可，顺序不能乱：

```
背景（为什么需要）→ 原理（怎么工作的）→ 使用（怎么写代码）
```

---

## 落地原则

### 1. 每个核心概念都要回答三个问题

| 问题 | 对应 | 举例 |
|------|------|------|
| **Why** — 为什么需要它？ | 背景 / 痛点 | 没有 IoC 时，对象创建和管理有什么问题？ |
| **How** — 它怎么工作的？ | 原理 / 机制 | BeanFactory 的 refresh() 做了什么？ |
| **What** — 怎么用它？ | 实战 / 代码 | @Autowired 背后发生了什么？ |

### 2. 内容结构模板

每章（或每节）按这个节奏展开：

```
① 场景引入 — 一个真实的痛点或问题（3-5 行代码展示"没有它时的痛"）
② 解决思路 — 朴素方案 → 引出 Spring 的设计思路
③ 原理拆解 — 核心机制，用流程图/时序图辅助
④ 代码实战 — 最小可运行示例，先跑通再解释
⑤ 避坑清单 — 高频踩坑点 + 排查方法
⑥ 小结 — 一句话总结本节核心
```

### 3. 三条红线

- ❌ 不堆砌注解 — 不做"注解字典"，每个注解都要说清它做了什么
- ❌ 不脱离场景 — 不讲"纯理论"，每个概念都要绑定一个真实开发场景
- ❌ 不背诵式教学 — 不做"面试八股"，要讲"工程理解"

### 4. 代码风格

- 先跑通，再解释 — 不要先贴 20 行代码再逐行讲解
- 对比式 — "没有 X 时的代码" vs "有了 X 时的代码"
- 最小示例 — 一个场景只展示一个核心点，不混搭

### 5. 图解优先

- 流程类 → 时序图 / 流程图
- 架构类 → 组件图 / 分层图
- 对比类 → 表格 / 左右对照

---

## 每卷的核心问题

| 卷 | 核心问题 |
|----|----------|
| 一 Spring 核心 | 对象为什么不能自己 new？容器到底帮了什么忙？ |
| 二 Spring AOP | 重复代码到处拷贝怎么办？代理是怎么做到"不改原代码"的？ |
| 三 Spring MVC | 一个 HTTP 请求怎么变成 Java 方法调用的？ |
| 四 Spring Boot | 为什么加个注解就能跑？自动配置到底配置了什么？ |
| 五 Spring Data | 每个数据库都写一遍 CRUD，能不能统一？ |
| 六 Spring Cloud | 服务多了怎么互相找到？挂了怎么办？ |
| 七 Spring 生态 | 安全、批处理、原生化——Spring 的能力边界在哪？ |
